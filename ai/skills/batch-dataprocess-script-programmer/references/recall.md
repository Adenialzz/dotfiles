
# Recall

如果在进行批量数据处理时，涉及向量模型召回，有以下注意事项：

## 输入参数

召回检索需要支持以下输入参数：

- `--retrieval_model`：召回检索所用的模型或服务
- `--topk`：召回相似度最高的结果个数
- `--round`：输出相似度分数时保留小数的位数，默认为 3

## 输出格式

在完成 topk 召回后，用户偏好这种输出格式：每一个 query 占 topk 行，每行包括：

- `retrieval_result`：召回结果
- `rank`：标识当前行是第几个候选
- `score`：召回分数
